apiVersion: v1
kind: Service
metadata:
  name: {{ include "k8s-express-api.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "k8s-express-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "k8s-express-api.selectorLabels" . | nindent 4 }}
